# -*- coding: utf-8 -*-
import re

story_property_permitted_dict = dict(
	story_title = [],
	...
)

story_output_propertys = ["storyTitle"...]


subModuleReDict = dict(
    pause = pause_re_list, # 暂停
    resume = resume_re_list, # 恢复
    stop = stop_re_list, # 停止
    next = next_re_list, # 下一个
    next_chapter = next_chapter_re_list, # 下一章
    prev = prev_re_list, # 上一个
    prev_chapter = prev_chapter_re_list, # 上一章
    favor = favor_re_list, # 加入红心列表
    repeat_one = repeat_re_list # 章节循环
)# value已删

slugify_re_list = [...
]
